# 📦 Generador de Albaranes con Supabase

![React](https://img.shields.io/badge/React-19-blue?logo=react&logoColor=white)
![Vite](https://img.shields.io/badge/Vite-7-purple?logo=vite&logoColor=white)
![Supabase](https://img.shields.io/badge/Supabase-Database-green?logo=supabase&logoColor=white)
![Tailwind CSS](https://img.shields.io/badge/Tailwind-CSS-38B2AC?logo=tailwind-css&logoColor=white)
![Licencia](https://img.shields.io/badge/Licencia-MIT-yellow)

Una aplicación web moderna y **responsiva** diseñada para digitalizar la gestión de albaranes de entrega. Permite crear documentos profesionales, calcular totales automáticamente, imprimirlos en PDF y almacenar todo el historial en la nube de forma segura.

---

## ✨ Características Principales

* 🔄 **Autonumeración Inteligente:** El sistema detecta automáticamente el último albarán del año y asigna el siguiente número (ej: `2025-001`, `2025-002`...).
* ☁️ **Persistencia en la Nube:** Integración nativa con **Supabase** para guardar clientes, líneas de producto y totales en tiempo real.
* 🖨️ **Modo Impresión PDF:** Estilos CSS optimizados (`@media print`) para generar documentos limpios, ocultando la interfaz de usuario al imprimir.
* 📦 **Catálogo de Productos:** Carga dinámica de productos desde la base de datos para autocompletar conceptos y precios.
* 📱 **Diseño Responsive:** Interfaz adaptada tanto para móviles como para escritorio usando **Tailwind CSS**.
* 🧮 **Cálculos Automáticos:** Suma de líneas, cálculo de base imponible e IVA (21%) al instante.

---

## 🛠️ Stack Tecnológico

* **Frontend:** [React 19](https://react.dev/) + [Vite](https://vitejs.dev/)
* **Estilos:** [Tailwind CSS](https://tailwindcss.com/)
* **Backend / Base de Datos:** [Supabase](https://supabase.com/) (PostgreSQL)
* **Linting:** ESLint

---

## 🚀 Instalación y Configuración

Sigue estos pasos para tener el proyecto corriendo en tu máquina local en minutos.

### 1. Clonar el repositorio

```bash
git clone [https://github.com/tu-usuario/creador-albaranes-supabase.git](https://github.com/tu-usuario/creador-albaranes-supabase.git)
cd creador-albaranes-supabase
npm install

### 2. Configurar Supabase
Crea un nuevo proyecto en Supabase y ejecuta el siguiente script en el SQL Editor para crear las tablas necesarias:

-- 1. Tabla de Productos (Catálogo)
create table productos (
  id bigint generated by default as identity primary key,
  created_at timestamp with time zone default timezone('utc'::text, now()) not null,
  nombre text not null,
  precio numeric not null
);

-- 2. Tabla de Albaranes (Cabeceras)
create table albaranes (
  id bigint generated by default as identity primary key,
  created_at timestamp with time zone default timezone('utc'::text, now()) not null,
  numero_albaran text unique not null,
  cliente_nombre text,
  cliente_nif text,
  cliente_direccion text,
  fecha text,
  observaciones text,
  total numeric
);

-- 3. Tabla de Líneas de Albarán (Detalles)
create table lineas_albaran (
  id bigint generated by default as identity primary key,
  created_at timestamp with time zone default timezone('utc'::text, now()) not null,
  albaran_id bigint references albaranes(id) on delete cascade,
  concepto text,
  cantidad numeric,
  precio numeric,
  total_linea numeric
);

-- (Opcional) Insertar datos de prueba
insert into productos (nombre, precio) values 
('Mano de obra', 35.00),
('Desplazamiento', 20.00),
('Filtro de agua', 15.50);

### 3. Variables de Entorno
Renombra o crea un archivo .env en la raíz del proyecto y añade tus credenciales de Supabase:

VITE_SUPABASE_URL=[https://tu-proyecto.supabase.co](https://tu-proyecto.supabase.co)
VITE_SUPABASE_ANON_KEY=tu-clave-anonima-publica

### 4. Ejecutar el Proyecto

npm run dev

Abre tu navegador en http://localhost:5173 y ¡listo! 🎉
```

## 📂 Estructura del Proyecto

/
├── public/              # Archivos estáticos (favicon, etc.)
├── src/
│   ├── assets/          # Imágenes y logotipos
│   ├── App.jsx          # Lógica principal de la aplicación
│   ├── supabaseClient.js # Cliente de conexión a Supabase
│   ├── index.css        # Estilos globales y reglas de impresión
│   └── main.jsx         # Punto de entrada
├── eslint.config.js     # Configuración de calidad de código
└── package.json         # Dependencias del proyecto

## 🤝 Contribución
¡Las contribuciones son bienvenidas! Si tienes ideas para mejorar este generador de albaranes:

1. Haz un Fork del repositorio.

2. Crea una rama con tu nueva funcionalidad (git checkout -b feature/NuevaFuncionalidad).

3. Haz Commit de tus cambios (git commit -m 'Añadir nueva funcionalidad').

4. Haz Push a la rama (git push origin feature/NuevaFuncionalidad).

5. Abre un Pull Request.

## 📄 Licencia
Este proyecto está bajo la Licencia MIT. Consulta el archivo LICENSE para más detalles.

<p align="center"> Hecho con ❤️ y código limpio </p>